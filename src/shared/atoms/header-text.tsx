import React from "react";
import styled from "styled-components";

const TopText = styled.h1`
  text-align: center;
  font-family: Helvetica-Neue-Bold, sans-serif;
  font-size: 64px;
  color: #000;
  margin: 0;
  padding: 40px 0 0;
`;
export const HeaderText = () => {
  return <TopText>ONLY.</TopText>;
};
